//! sentinel — Autonomous agent supervisor daemon
//! Spawns, monitors, and restarts agent worker processes.
//! Manages triggers, heartbeats, and resource limits.

fn main() {
    eprintln!("sentinel: not yet implemented");
    std::process::exit(1);
}
